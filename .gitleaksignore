# Known gitleaks false positives, suppressed by fingerprint (one per line).
#
# The generated client's multi-line import block: gitleaks' generic-api-key
# heuristic misreads the last import item against the module path
# (.../lib/api/generated/client) as an API key assignment.
# Global fingerprint (file:rule-id:start-line, no commit): a 4-field commit
# fingerprint must match the finding's exact commit SHA, which changes on
# every merge and would need a new entry each time.
tests/generated-client.integration.spec.ts:generic-api-key:13
